export const signup = (inputs) => {
  // throw new Error("Email exists", {cause: {status: 409}})
  return [];
};

export const login = (inputs) => {
  // throw new Error("Not exists", {cause: {status: 404}})
  return [];
};
